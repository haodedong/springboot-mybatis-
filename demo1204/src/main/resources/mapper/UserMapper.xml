<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.demo1204.mapper.UserMapper" >

	<!-- <select id="findUser" parameterType="com.example.demo1204.domain.User" resultType="java.lang.Integer">
		select count(*) from user where name=#{name} and password =#{password}
	</select> -->
	<select id="findUser" parameterType="com.example.demo1204.domain.User" resultType="java.lang.Integer">
		select count(*) from user where name=#{name} and password =#{password}
	</select>
</mapper>